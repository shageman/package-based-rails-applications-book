# This file contains a list of dependencies that are not part of the long term plan for app/packages/games.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations app/packages/games
---
".":
  "::ApplicationRecord":
    violations:
    - dependency
    files:
    - app/packages/games/models/game.rb
app/packages/teams:
  "::Team":
    violations:
    - dependency
    files:
    - app/packages/games/models/game.rb
