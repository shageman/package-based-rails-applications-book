platform: linux
image_resource:
  type: registry-image
  source:
    repository: shageman/ruby33-sorbet
    tag: '1.6'

inputs:
- name: generator-scripts-repo

outputs:
- name: code_output

run:
  path: generator-scripts-repo/generator_scripts/ci/generate-code.sh
