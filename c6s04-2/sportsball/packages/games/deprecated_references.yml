# This file contains a list of dependencies that are not part of the long term plan for packages/games.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/games
---
packages/teams:
  "::TeamRecord":
    violations:
    - privacy
    files:
    - packages/games/app/models/game.rb
