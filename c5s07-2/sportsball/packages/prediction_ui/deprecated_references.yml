# This file contains a list of dependencies that are not part of the long term plan for packages/prediction_ui.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/prediction_ui
---
packages/predictor:
  "::Predictor":
    violations:
    - dependency
    files:
    - packages/prediction_ui/app/services/prediction_ui.rb
