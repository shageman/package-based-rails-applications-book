# This file contains a list of dependencies that are not part of the long term plan for packages/predictor.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# packwerk update-deprecations packages/predictor
---
packages/games:
  "::Game":
    violations:
    - dependency
    files:
    - packages/predictor/app/models/prediction_needed_subscriber.rb
packages/teams:
  "::Team":
    violations:
    - dependency
    files:
    - packages/predictor/app/models/prediction_needed_subscriber.rb
