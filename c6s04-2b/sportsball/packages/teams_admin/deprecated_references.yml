# This file contains a list of dependencies that are not part of the long term plan for packages/teams_admin.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-deprecations packages/teams_admin
---
packages/teams:
  "::TeamRecord":
    violations:
    - privacy
    files:
    - packages/teams_admin/spec/requests/teams_spec.rb
